<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="cn.javat.ssm.mapper.MessageMapper">
    
    <resultMap id="MessageResultMap" type="cn.javat.ssm.entity.Message">
        <id column="id" property="id"/>
        <result column="title" property="title"/>
        <result column="content" property="content"/>
        <result column="like_count" property="likeCount"/>
        <result column="creator_id" property="creatorId"/>
        <result column="create_time" property="createTime"/>
        <result column="update_time" property="updateTime"/>
        <result column="status" property="status"/>
        <result column="is_deleted" property="isDeleted"/>
    </resultMap>
    
    <sql id="table_columns">
        id, title, content, like_count, creator_id, create_time, update_time, status, is_deleted
    </sql>
    
    <select id="selectById" parameterType="long" resultMap="MessageResultMap">
        SELECT 
        <include refid="table_columns"/>
        FROM message
        WHERE id = #{id} AND is_deleted = 0 AND status = 0
    </select>
    
    <select id="selectList" resultMap="MessageResultMap">
        SELECT 
        <include refid="table_columns"/>
        FROM message
        WHERE is_deleted = 0 AND status = 0
        ORDER BY create_time DESC
        LIMIT #{offset}, #{limit}
    </select>
    
    <select id="selectByKeyword" parameterType="string" resultMap="MessageResultMap">
        SELECT 
        <include refid="table_columns"/>
        FROM message
        WHERE is_deleted = 0 AND status = 0
        AND (title LIKE CONCAT('%', #{keyword}, '%') OR content LIKE CONCAT('%', #{keyword}, '%'))
        ORDER BY create_time DESC
        LIMIT #{offset}, #{limit}
    </select>
    
    <insert id="insert" parameterType="cn.javat.ssm.entity.Message" useGeneratedKeys="true" keyProperty="id">
        INSERT INTO message(title, content, like_count, creator_id, create_time, update_time, status, is_deleted)
        VALUES(#{title}, #{content}, #{likeCount}, #{creatorId}, #{createTime}, #{updateTime}, #{status}, #{isDeleted})
    </insert>
    
    <update id="update" parameterType="cn.javat.ssm.entity.Message">
        UPDATE message
        <set>
            <if test="title != null">title = #{title},</if>
            <if test="content != null">content = #{content},</if>
            <if test="likeCount != null">like_count = #{likeCount},</if>
            <if test="creatorId != null">creator_id = #{creatorId},</if>
            <if test="createTime != null">create_time = #{createTime},</if>
            <if test="updateTime != null">update_time = #{updateTime},</if>
            <if test="status != null">status = #{status},</if>
            <if test="isDeleted != null">is_deleted = #{isDeleted}</if>
        </set>
        WHERE id = #{id}
    </update>
    
    <update id="deleteById" parameterType="long">
        UPDATE message SET is_deleted = 1 WHERE id = #{id}
    </update>
    
    <update id="updateLikeCount" parameterType="map">
        UPDATE message SET like_count = like_count + #{count} WHERE id = #{id} AND like_count + #{count} >= 0
    </update>
    
</mapper>