<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="cn.javat.ssm.mapper.ReportMapper">
    
    <resultMap id="ReportResultMap" type="cn.javat.ssm.entity.Report">
        <id column="id" property="id"/>
        <result column="user_id" property="userId"/>
        <result column="message_id" property="messageId"/>
        <result column="reason" property="reason"/>
        <result column="create_time" property="createTime"/>
        <result column="audit_status" property="auditStatus"/>
        <result column="audit_user_id" property="auditUserId"/>
        <result column="audit_time" property="auditTime"/>
    </resultMap>
    
    <sql id="table_columns">
        id, user_id, message_id, reason, create_time, audit_status, audit_user_id, audit_time
    </sql>
    
    <select id="selectById" parameterType="long" resultMap="ReportResultMap">
        SELECT 
        <include refid="table_columns"/>
        FROM report
        WHERE id = #{id}
    </select>
    
    <insert id="insert" parameterType="cn.javat.ssm.entity.Report" useGeneratedKeys="true" keyProperty="id">
        INSERT INTO report(user_id, message_id, reason, create_time, audit_status, audit_user_id, audit_time)
        VALUES(#{userId}, #{messageId}, #{reason}, #{createTime}, #{auditStatus}, #{auditUserId}, #{auditTime})
    </insert>
    
    <update id="updateAuditStatus" parameterType="map">
        UPDATE report 
        SET audit_status = #{auditStatus}, audit_user_id = #{auditUserId}, audit_time = #{auditTime}
        WHERE id = #{id}
    </update>
    
</mapper>