<?xml version="1.0" encoding="UTF-8"?>
<beans xmlns="http://www.springframework.org/schema/beans"
       xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance"
       xmlns:context="http://www.springframework.org/schema/context"
       xmlns:mvc="http://www.springframework.org/schema/mvc"
       xsi:schemaLocation="
        http://www.springframework.org/schema/beans
        http://www.springframework.org/schema/beans/spring-beans.xsd
        http://www.springframework.org/schema/context
        http://www.springframework.org/schema/context/spring-context.xsd
        http://www.springframework.org/schema/mvc
        http://www.springframework.org/schema/mvc/spring-mvc.xsd">

    <!-- 扫描 Interceptor 拦截器层 -->
    <context:component-scan base-package="cn.javat.ssm.interceptor"/>
    
    <!-- 扫描 Util 工具类组件 -->
    <context:component-scan base-package="cn.javat.ssm.util"/>

    <!-- 配置拦截器 -->
    <mvc:interceptors>
        <!-- 登录拦截器 -->
        <bean class="cn.javat.ssm.interceptor.LoginInterceptor"/>
    </mvc:interceptors>


    <!-- 扫描 Controller 层 -->
    <context:component-scan base-package="cn.javat.ssm.controller"/>

    <!-- 开启注解驱动，自动注册 JSON 转换器 -->
    <mvc:annotation-driven/>

    <!-- 配置跨域请求（前后端分离必配） -->
    <mvc:cors>
        <mvc:mapping path="/**"
                     allowed-origins="*"
                     allowed-methods="GET,POST,PUT,DELETE"
                     allowed-headers="*"/>
    </mvc:cors>

    <!-- 放行静态资源（若后端无静态资源可省略） -->
    <mvc:default-servlet-handler/>
</beans>